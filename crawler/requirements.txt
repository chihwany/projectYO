# HTTP 요청 / HTML 파싱
requests==2.32.5
aiohttp>=3.9.0
beautifulsoup4==4.14.3
lxml>=6.0.2  # Python 3.14 지원: pip install lxml --pre 로 설치

# 웹 서버
flask==3.1.3

# 스케줄러 (Phase 2-4에서 사용)
APScheduler==3.11.2

# Redis 클라이언트 (Phase 2-1에서 사용)
redis==7.2.1

# 환경변수
python-dotenv==1.2.2
